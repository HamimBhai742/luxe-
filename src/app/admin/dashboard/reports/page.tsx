import AdminReportsClient from "@/components/dashboard/admin/AdminReportsClient";

export const metadata = {
  title: "Reports & Tickets - Aura Admin",
  description: "Manage customer support inquiries, solve technical issues, and dispatch status emails.",
};

export default function AdminReportsPage() {
  return <AdminReportsClient />;
}
